#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	for(char i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for(char i = 'A'; i<= 'Z'; i++)
	{
		putchar(i);
	}

	putchar('\n');
	return (0);
}
