#include <stdio.h>

/**
* main - Entry point
*description:- print all the digits
* Return:0
*/
int main(void)
{
	char i;

	for (i = '0'; i <= '9'; ++i)
	putchar (x);
	for (i = 'a'; i <= 'f'; ++i)
	putchar (i);

	putchar('\n');

	return (0);
}
